{
  "extends": "@tsconfig/node22/tsconfig.json",
  "compilerOptions": {
    "types": ["vitest/globals"],
    // Required for extensionless package subpath imports used by the test tools.
    "moduleResolution": "bundler",
    "module": "ESNext"
  }
}
