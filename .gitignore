# Build artifacts
/target
/crates/**/target
/examples/target
/examples/**/target

# hooks-build outputs
out/
*.wasm

# Keep binary test fixtures
!crates/hooks-build/tests/fixtures/**/*.wasm

# OS / editor noise
.DS_Store

# e2e: xrpld-netgen's standalone-node runtime state (db, docker-compose,
# downloaded xahaud binary), dropped in the cwd `up:standalone` is run from
/xahau/
